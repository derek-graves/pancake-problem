import "./style.css";
import loadSandbox from "./load-sandbox";

loadSandbox();

/* 
const fullStack = (function () {
  const init = {
    stack: new Stack(defaultOrder),
  };
  return Object.seal(init);
})();
*/
